package Generic;
import.org.selenium.firefox.FirefoxDriver;
public class BaseTest implements Constant {
	P
	public  void openapp()
	{
		System.setProperty(GECKO_KEY,GECKO_VALUE);
	 driver=new FirefoxDriver();
	}

}
